<div class="fl w-100 pa2 mv5">
	<%= form_for(current_user, html: { class: 'measure center' }) do |f| %>
		<h3 class="mb2">add your name</h3>

		<%= f.text_field :name, autofocus: true, placeholder: 'name', class: "w-100 mv3" %>

		<%= f.submit "submit", class: 'button primary-button fr' %>
	<% end %>
</div>
